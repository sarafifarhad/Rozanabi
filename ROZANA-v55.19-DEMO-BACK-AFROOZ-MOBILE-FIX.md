# Rozana v55.19 — Demo Back Button + Afrooz Mobile Fix

## Changes
- Unified the `Back to Landing Page Website` button on the remaining Landing Page Website demo pages:
  - Afrooz Perfumes
  - Maya’s Collection
  - Eventona
  - Dokon Collective
  - Beauty clinic
- All remaining demo pages now use the same `assets/css/demo-return.css` button style.
- Fixed old mixed button label text from `Landing Page Service` to `Landing Page Website`.
- Fixed the mobile `Order on WhatsApp` button inside the Afrooz product detail modal so it has a proper mobile size and does not feel oversized or cramped.
- Fixed the Afrooz product detail modal image area on mobile so the image uses the top image area properly instead of appearing too small.
- Updated version and cache references to `55.19`.
